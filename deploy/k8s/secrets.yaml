apiVersion: v1
kind: Secret
metadata:
  name: stockinsight-secrets
  namespace: stockinsight
type: Opaque
stringData:
  MONGODB_URI: mongodb://stockinsight:replace-me@mongodb:27017
  JWT_SECRET: replace-with-long-random-secret
  GEMINI_API_KEY: replace-with-real-gemini-key
